# CLAUDE.md — 參拾 aChi 部落格專案規則

achi 的學習部落格。純 HTML / CSS，沒有框架、沒有 JavaScript、沒有建置工具。
一律使用**繁體中文（台灣用語）**。動手前先讀完這份。

網址（尚未上線）：`https://hugo0963.github.io/blog/`

---

## 這個站是寫給誰的

**第一順位是 achi 自己**，不是陌生初學者。

- 出現「Claude Code」「技能」這類名詞時**不要停下來解釋**。他自己看得懂，補解釋會讓一篇從 15 分鐘變成 40 分鐘
- 累積十篇以後，再挑幾篇改寫成初學者版本。**在那之前不要主動提議做初學者版**
- 目標頻率：**一週至少一篇**。任何會讓單篇成本超過 30 分鐘的提議，先擋下來

## 內容形態

- 多數是**心得文**（文字為主）；偶爾一篇附可玩的作品
- **現在不要做作品縮圖、預覽圖、demo 嵌入**——那會讓每篇成本翻倍。等真的有第二個作品再說

## 檔案結構

```
index.html          首頁（文章卡片列表，時間倒序）
_template.html      新文章範本 → 複製到 posts/
posts/              文章，檔名 YYYY-MM-DD-標題.html
css/style.css       唯一一份樣式表
screenshots/        驗證用截圖
DEVLOG.md           開發紀錄（隨手寫，會公開）
```

## 樣式規則

- **顏色一律改 `css/style.css` 最上面的兩組色票**，不要在其他地方寫死顏色
  - 色票 1 = 淺色（預設，米白 `#faf9f7`）
  - 色票 2 = `@media (prefers-color-scheme: dark)`，系統深色模式時自動套用
  - **改了其中一組，一定要檢查另一組**，不然深色模式會壞掉
- 不要引入 CSS 框架、字型 CDN、JavaScript。**這個限制是刻意的**，不要提議繞過
- 新文章一律複製 `_template.html`，不要從零手寫

## 標籤

- 標籤**可以自由新增**（achi 決定的）
- 但每次要標新標籤之前，**先列出首頁已經有哪些標籤給他看**，避免長出「心得／隨筆／雜記」這種同義詞

## 與 Obsidian vault 的分工

- **vault（`~/Obsidian/學習紀錄/`）放原始素材**：潦草、私人、含抱怨，5 分鐘寫完，frontmatter 用 `project_path` 指回這個專案
- **部落格放成品**：整理過、可公開的版本
- **絕對不要把同一份內容同時放兩邊**，會不同步

## 提交與公開

- 這個倉庫會是**公開**的，包含 `DEVLOG.md`。寫任何檔案前先假設全世界看得到
- 這個專案的 git email 用 `hugo0963@users.noreply.github.com`，**不要用真實 email 提交**
- 不要把密碼、token、API key 寫進任何檔案

## 沿用 vault 的規則

`~/Obsidian/CLAUDE.md` 裡的這幾條在這裡同樣有效：

- **收工時主動產出「三行草稿 + 下次從這裡開始」**，不用等他要求
- **不要主動擴張系統**。做完交代的事就停，想到的事用一行寫在最後標「未執行的想法」
- 例外（必須主動說）：資料遺失風險、他正在做白工、指示與規則衝突
